@{
    ViewData["Title"] = "Acceso restringido";
}

<section class="panel access-denied">
    <h1>Acceso restringido</h1>
    <p>Tu rol actual no tiene permisos para esta operacion.</p>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Volver al panel</a>
</section>
